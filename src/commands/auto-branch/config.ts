import { ConfigBaseCommand } from "../../base-commands/ConfigBaseCommand.js";

export default class AutoBranchConfig extends ConfigBaseCommand {
    static description = "Opens up the configuration for the auto-branch command.";
}
