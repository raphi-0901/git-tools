import { ConfigBaseCommand } from "../../base-commands/ConfigBaseCommand.js";

export default class AutoCommitConfig extends ConfigBaseCommand {
    static description = "Opens up the configuration for the auto-commit command.";
}
