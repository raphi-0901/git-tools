feat: support custom elements without shadowRoot

Add option to define custom elements with shadowRoot disabled, parse and render slots manually, and adjust rendering logic to use a generic root element. This enables usage of custom elements in light DOM and improves slot handling.
